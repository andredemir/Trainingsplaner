package com.haw.srs.customerservice.enums;

/**
 * A class with all the status types an order can have
 */
public enum StatusType {
    VORBEREITUNG, VERSENDET, OFFEN, UNBEARBEITET, BEARBEITET, PRODUCT_OK, PRODUCT_NOT_OK
}
